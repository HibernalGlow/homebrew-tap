# 2026-09-19

- 新增 cask `lume-app`：上游 hugomyb/Lume（Tauri，universal dmg，v1.2.0）。
  - token 原名 `lume` 与 homebrew/core 同名公式冲突 → 改名 `lume-app`（`name`/`homepage` 仍写 Lume）。
  - 上游 ad-hoc 签名缺陷（无 _CodeSignature）→ 加 caveats 重签名修复；同步把 /Applications/Lume.app 加进本地 LaunchAgent 的 DEFAULT_APPS 与 WatchPaths。
  - VM 数据在 ~/Library/Application Support/lume，zap 刻意不列该目录（防误删用户虚拟机）。
  - 顺手修 micyou.rb：`depends_on macos: :big_sur` → `depends_on :macos`（CI 的 OSDependsOn 现判 Big Sur 冗余）。
  - CI（tests.yml）全绿：audit --strict --online 实测下载校验了 dmg 的 sha256，install/uninstall --zap 通过。
